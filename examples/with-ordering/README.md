# Ordered Pub/Sub Subscription

Subscription with message ordering enabled, ensuring messages with the same ordering key are delivered in order.

## Usage

```bash
terraform init -backend=false
terraform validate
```
