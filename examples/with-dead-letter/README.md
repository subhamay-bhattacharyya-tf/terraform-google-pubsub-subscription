# Dead-Letter Pub/Sub Subscription

Subscription with a dead-letter policy that forwards undeliverable messages to a dead-letter topic after a configurable number of delivery attempts.

## Usage

```bash
terraform init -backend=false
terraform validate
```
