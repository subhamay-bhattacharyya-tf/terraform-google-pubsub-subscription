# Complete Pub/Sub Subscription

Full-featured subscription with dead-letter policy, message filter, extended retention, message ordering, expiration policy, and resource labels.

## Usage

```bash
terraform init -backend=false
terraform validate
```
