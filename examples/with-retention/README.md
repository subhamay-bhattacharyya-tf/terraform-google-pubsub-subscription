# Extended Retention Pub/Sub Subscription

Subscription with 14-day message retention and acknowledged-message retention enabled for replay scenarios.

## Usage

```bash
terraform init -backend=false
terraform validate
```
