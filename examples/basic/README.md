# Basic Pub/Sub Subscription

Minimal example: creates a subscription with all defaults (10s ack deadline, 7-day retention, no filter).

## Usage

```bash
terraform init -backend=false
terraform validate
```
