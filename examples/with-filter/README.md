# Filtered Pub/Sub Subscription

Subscription with a message filter that only delivers messages matching a specific attribute expression.

## Usage

```bash
terraform init -backend=false
terraform validate
```
